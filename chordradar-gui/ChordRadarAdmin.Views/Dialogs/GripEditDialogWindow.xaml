<Window x:Class="ChordRadarAdmin.Views.Dialogs.GripEditDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Grip Editor"
        Width="380"
        Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Grip Editor" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ErrorBrush}" Margin="0,0,0,8" />
            <TextBlock Text="Strings" Margin="0,0,0,4" />
            <TextBox Text="{Binding Strings, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource ButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource ButtonStyle}" Background="{DynamicResource ErrorBrush}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
