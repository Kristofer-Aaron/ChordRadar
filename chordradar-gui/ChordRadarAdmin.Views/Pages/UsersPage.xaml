<UserControl x:Class="ChordRadarAdmin.Views.Pages.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource SecondaryBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="280" Margin="0,0,8,0" Style="{StaticResource TextBoxStyle}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Clear" Command="{Binding ClearFilterCommand}" Style="{StaticResource ButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource ButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Add" Command="{Binding AddCommand}" Style="{StaticResource ButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Edit" Command="{Binding EditCommand}" Style="{StaticResource ButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource ButtonStyle}" Background="{DynamicResource ErrorBrush}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DataGrid ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Style="{StaticResource DataGridStyle}"
                      Grid.Column="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" />
                    <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" Width="*" />
                    <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="*" />
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                    <DataGridCheckBoxColumn Header="Email Verified" Binding="{Binding EmailVerified}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1"
                    Margin="12,0,0,0"
                    Padding="16"
                    Background="{DynamicResource SecondaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="User Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" />
                        <TextBlock Text="{Binding SelectedItem.UserName, FallbackValue=Select a user}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />

                        <TextBlock Text="First Name" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.FirstName}" Margin="0,0,0,8" />

                        <TextBlock Text="Last Name" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.LastName}" Margin="0,0,0,8" />

                        <TextBlock Text="Password Changed At" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.PasswordChangedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,0,0,8" />

                        <TextBlock Text="Two-Factor Enabled" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.TwoFactorEnabled}" Margin="0,0,0,8" />

                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem.TwoFactorEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="Two-Factor Method" FontWeight="Bold" />
                            <TextBlock Text="{Binding SelectedItem.TwoFactorMethod}" Margin="0,0,0,8" />
                        </StackPanel>

                        <TextBlock Text="Account Created At" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.AccountCreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,0,0,8" />

                        <TextBlock Text="Last Login At" FontWeight="Bold" />
                        <TextBlock Text="{Binding SelectedItem.LastLoginAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Margin="0,0,0,8" />

                        <Expander Header="Preferences (JSON)" IsExpanded="False" Margin="0,8,0,0">
                            <TextBlock Text="{Binding SelectedItem.PreferencesDisplay}" TextWrapping="Wrap" FontFamily="Consolas" />
                        </Expander>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
